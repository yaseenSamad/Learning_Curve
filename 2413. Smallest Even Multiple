/**
 * @param {number} n
 * @return {number}
 */
var smallestEvenMultiple = function(n) {

    function gcd(a, b) {
        return b === 0 ? a : gcd(b, a % b);
    }

    return (2 * n) / gcd(2, n);

    // return n % 2 === 0 ? n : n * 2;
};
